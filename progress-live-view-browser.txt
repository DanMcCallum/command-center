# Progress — Self-Hosted Live-View Login Capture (specs/live-view-browser.md)

## Iteration 1 - US-001: Add capture-mode config + login-success signal
- What was implemented:
  - All six platforms in config/posting-platforms.json gained capture: "live-view"
    and a login_success signal (redirect_off matching each platform's login path;
    land_com uses "/account/login", the rest "/login"). _doc updated to explain both fields.
  - PlatformConfig (workers/posting/capture-login.ts) and PostingPlatformConfig
    (dashboard/app/api/posting-platforms/route.ts) gained optional capture +
    login_success fields; both export/declare a LoginSuccessSignal shape
    { cookie?: string, redirect_off?: string }.
  - GET /api/posting-platforms needed NO code change — it returns the parsed
    config.platforms verbatim, so new config fields flow through automatically.
- Files changed: config/posting-platforms.json, workers/posting/capture-login.ts,
  dashboard/app/api/posting-platforms/route.ts, specs/live-view-browser.md
- Learnings for future iterations:
  - Typecheck commands: `cd workers/posting && npm run typecheck` (tsc -p tsconfig.json)
    and `cd dashboard && npx tsc --noEmit`. Both pass clean as of this iteration.
  - The working tree had a pre-existing unrelated change: landmodo login_url was
    updated from /users/sign_in to /login (kept; committed with US-001 since the
    redirect_off signal depends on the current login path).
  - login_success.redirect_off values are best-effort for the four disabled platforms
    (same caveat as their placeholder URLs) — verify before enabling a platform.
  - Quick config sanity check: node -e "require('.../posting-platforms.json')" and
    print fields — cheaper than spinning up the dashboard.
  - US-002 next: workers/posting/capture/session.ts; reuse AUTH_DIR from
    post-common.ts, no file writes yet.
---

## Iteration 2 - US-002: Capture-session manager (headed Chromium under a display)
- What was implemented:
  - New workers/posting/capture/session.ts: startSession(platformKey) validates
    the platform (enabled + capture: "live-view", one-line errors), launches
    chromium.launch({ headless: false }) (inherits process.env.DISPLAY),
    opens context+page, navigates to login_url, tracks the session in an
    in-memory Map keyed by a random hex sessionId. getSession(id) exposes the
    live context/page; closeSession(id) closes the browser and forgets it
    (no-op for unknown ids). The session record carries authPath computed from
    post-common's AUTH_DIR (for US-004's storageState export) — no file writes.
  - post-common.ts PlatformConfig gained the optional capture + login_success
    fields (exported LoginSuccessSignal) so loadPlatformConfig covers US-001's
    config shape; capture-login.ts keeps its own local copy of the interface.
  - tsconfig.json include widened to ["*.ts", "capture/**/*.ts"] — the old
    ["*.ts"] would have silently skipped the new subdirectory in typecheck.
- Files changed: workers/posting/capture/session.ts (new),
  workers/posting/post-common.ts, workers/posting/tsconfig.json,
  specs/live-view-browser.md
- Learnings for future iterations:
  - npm run typecheck AND npm test both pass (12/12). The test script compiles
    with --outDir /tmp/posting-test-build and globs only top-level *.test.js —
    US-004's capture/detect.test.ts will compile to
    /tmp/posting-test-build/capture/detect.test.js and NOT be picked up; the
    test script glob must become e.g. "/tmp/posting-test-build/**/*.test.js"
    (node --test accepts multiple patterns) when adding that test.
  - Rejection paths are runtime-verified without a browser via
    npx tsx -e "import { startSession } ..." — disabled platform and unknown
    platform both throw one-line errors (unknown comes from loadPlatformConfig).
  - No platform in config is capture: "cli" today (all six are live-view), so
    the not-live-view rejection branch is only type/logic-verified.
  - Startup failure after launch (e.g. goto throws) closes the browser before
    rethrowing — no orphaned Chromium.
  - US-003 next: capture/stream.ts (Xvfb -> x11vnc -> websockify -> noVNC),
    single-active-session enforcement lives THERE, needs CAPTURE_STREAM_SECRET
    (OT-3) for token signing and must fail closed naming missing binaries (OT-1).
---

## Iteration 3 - US-003: Streaming bridge (Xvfb -> x11vnc -> websockify -> noVNC)
- What was implemented:
  - New workers/posting/capture/stream.ts: startStream() checks the single-session
    slot (claimed as 'starting' at entry so concurrent starts fail closed), then
    secret -> binaries -> noVNC root (all BEFORE any spawn, so a missing dep never
    leaves a partial stream), then spawns Xvfb :99 (1280x800x24, -nolisten tcp),
    x11vnc (-rfbport 5901 -localhost -forever -shared -nopw), and websockify
    (--web <novncRoot> 6080 localhost:5901), each awaited ready (X socket file /
    TCP port poll, 10 s timeout, early-exit detection with stderr tail in the error).
  - Sets process.env.DISPLAY=:99 on success so session.ts's headed chromium.launch
    renders into the stream; stopStream() restores the prior DISPLAY and tears down
    in reverse order (SIGTERM, SIGKILL after 3 s).
  - Token: signStreamToken/verifyStreamToken — "<expiryMs>.<hmacSha256Hex>" over
    "live-view:<exp>", 10-min TTL, timingSafeEqual verify. getStreamSecret() reads
    CAPTURE_STREAM_SECRET from env, falling back to parsing project-root .env.local.
    Exported for US-005's server to verify tokens.
  - Fixed plumbing exported: CAPTURE_DISPLAY=':99', WS_PORT (env CAPTURE_WS_PORT,
    default 6080); VNC_PORT 5901 internal. noVNC root: NOVNC_ROOT env override,
    then /usr/share/novnc, /usr/share/webapps/novnc, /usr/local/share/novnc.
- Files changed: workers/posting/capture/stream.ts (new), specs/live-view-browser.md
- Learnings for future iterations:
  - This box has Xvfb + websockify installed but NOT x11vnc or noVNC, and no
    CAPTURE_STREAM_SECRET in .env.local — OT-1/OT-3 are still incomplete. All
    fail-closed paths were runtime-verified for real; the happy path was verified
    with a stub x11vnc (python TCP listener on 5901) + stub vnc.html on PATH,
    with REAL Xvfb + websockify: URL token present, second start rejected,
    vnc.html served (200) over websockify, headed Playwright Chromium launched
    onto :99 via the same launch call session.ts uses, teardown left zero orphans
    and restored DISPLAY. Stub-binary-on-PATH is a cheap reusable pattern for
    testing process orchestration without the real deps.
  - websockify's --web mode serves the static client AND bridges the WebSocket
    on ONE port — the noVNC URL uses path=websockify on the same port (6080).
  - .env.local lives at project root (not dashboard/); fetch-dialpad.sh sources
    it; stream.ts parses it directly since Node doesn't auto-load it.
  - US-004 next: capture/detect.ts (isLoggedIn predicate + checkSession), calls
    context.storageState + chmod 600, tears down session AND stream. Remember:
    package.json test glob is /tmp/posting-test-build/*.test.js — must widen to
    pick up capture/detect.test.js (node --test accepts multiple patterns).
---

## Iteration 4 - US-004: Login-success detection + storageState export
- What was implemented:
  - New workers/posting/capture/detect.ts: isLoggedIn(signal, {cookies, url})
    pure predicate — AND over the defined login_success signals (cookie name
    present; URL *pathname* no longer contains redirect_off, so a query-string
    mention like ?from=%2Flogin doesn't count); missing/empty signal or
    unparseable URL fails closed (false). checkSession(sessionId) reads the
    live context's cookies + page.url(); on success mkdirs the auth dir,
    context.storageState({ path: session.authPath }), chmod 600, then
    closeSession + stopStream. False result leaves the session alive.
    Nothing is ever logged (no cookie values, no state contents).
  - New workers/posting/capture/detect.test.ts: 8 node:test cases (cookie
    present/absent, redirect on/off/query-string/unparseable, combined
    signals, empty signal).
  - package.json test glob widened to also run
    /tmp/posting-test-build/capture/*.test.js (the gotcha from iteration 2).
- Files changed: workers/posting/capture/detect.ts (new),
  workers/posting/capture/detect.test.ts (new), workers/posting/package.json,
  specs/live-view-browser.md
- Learnings for future iterations:
  - detect.ts imports session.ts/stream.ts LAZILY (await import inside
    checkSession) — session.ts has a runtime `import { chromium }` and the
    compiled test build runs from /tmp where playwright can't resolve, so a
    static import would break `npm test` at load time. TS compiles dynamic
    import under CommonJS to a lazy require, which is exactly what's needed.
    Keep this pattern for any capture/* module that a unit test imports.
  - Typecheck + all 20 tests pass (12 prior + 8 new).
  - Runtime-verified checkSession end-to-end under xvfb-run with a LOCAL stub
    login server (http.createServer) + a transient _verify_stub platform
    appended to config/posting-platforms.json and restored in finally (git
    diff clean afterwards): false-on-login-page leaves the session alive;
    true-after-redirect writes storageState (keys cookies,origins) mode 600
    and tears the session down; unknown sessionId errors cleanly. Redirect
    session.authPath to /tmp before a success-path test — auth/*.json holds
    REAL sessions (landmodo + land_com exist, mode 600).
  - Real-site verification is flaky: page.goto('https://www.landmodo.com/login')
    timed out on the 'load' event under Xvfb (slow third-party subresources)
    even though curl gets a 200 in ~1.3 s. Prefer the local-stub pattern; if a
    real site is ever needed, expect to need waitUntil: 'domcontentloaded'.
  - US-005 next: capture/server.ts HTTP service on CAPTURE_PORT (default 4750):
    POST /capture/start, GET /capture/status?sessionId, POST /capture/cancel;
    guard with token derived from CAPTURE_STREAM_SECRET (stream.ts exports
    getStreamSecret/verifyStreamToken); fail closed at startup if secret
    absent; add "capture-server" script to package.json.
---
## Iteration 5 - US-005: Capture-server HTTP surface on the worker
- What was implemented:
  - New workers/posting/capture/server.ts: HTTP service on CAPTURE_PORT
    (env, default 4750, binds 127.0.0.1). POST /capture/start {platform}
    pre-validates (enabled + live-view) BEFORE spawning anything, checks
    isStreamActive() -> 409, then startStream() -> startSession() (session
    failure rolls back the stream). GET /capture/status?sessionId runs
    checkSession (404 unknown session, {loggedIn} otherwise). POST
    /capture/cancel {sessionId} closes session + stream. Every request
    needs "Authorization: Bearer <deriveApiToken(secret)>" where
    deriveApiToken = HMAC-SHA256("capture-api", CAPTURE_STREAM_SECRET),
    compared with timingSafeEqual; missing/wrong -> 401. Startup calls
    getStreamSecret() and exits 1 with the OT-3 message when absent.
    SIGINT/SIGTERM tear down the current session + stream. Bad JSON /
    missing fields -> 400 (readBodyOr400 helper).
  - session.ts: extracted requireLiveViewPlatform(platformKey) (validation
    startSession already did) so the server pre-validates cheaply; exported.
  - package.json: added "capture-server": "tsx capture/server.ts".
- Files changed: workers/posting/capture/server.ts (new),
  workers/posting/capture/session.ts, workers/posting/package.json,
  specs/live-view-browser.md
- Learnings for future iterations:
  - Runtime-verified end-to-end over HTTP with the stub-binary pattern
    (stub x11vnc node listener on 5901 + stub vnc.html + REAL Xvfb +
    websockify) plus a local stub login site whose /login meta-refreshes
    to /home after 4s, and a transient _verify_stub platform (config
    backed up + restored, git diff clean after): no/wrong token 401;
    bad JSON 400; unknown platform 400; start returns 32-hex sessionId +
    liveViewUrl with signed token; concurrent start 409; status false on
    /login leaves session alive; status true writes auth/<platform>.json
    mode 600 (keys cookies,origins) and tears down (later status 404);
    cancel 200 + full teardown; zero orphan processes.
  - GOTCHA: killing a backgrounded `npx tsx ...` by its $! kills npx but
    NOT the node child — the server keeps running and holds the port
    (next start dies EADDRINUSE while curls silently hit the OLD code).
    Use fuser -k <port>/tcp to reliably free it.
  - pgrep -f self-matches the Bash tool's wrapper (the eval'd command
    string contains the pattern) — use pgrep -x for orphan checks.
  - deriveApiToken is exported from capture/server.ts; US-006's dashboard
    proxy can copy the 3-line HMAC derivation (dashboard can't import
    workers modules) or the operator puts the derived token in dashboard
    env. Capture-server env: CAPTURE_PORT, CAPTURE_STREAM_SECRET (or
    project-root .env.local), NOVNC_ROOT/CAPTURE_WS_PORT via stream.ts.
  - US-006 next: dashboard/app/api/posting-auth/connect/route.ts +
    status/route.ts proxying the capture-server (base URL + token from
    env, e.g. CAPTURE_SERVER_URL); reject non-live-view platforms; keep
    read-only GET /api/posting-auth unchanged; dashboard typecheck is
    npx tsc --noEmit.
---
## Iteration 6 - US-006: Dashboard connect + status proxy routes
- What was implemented:
  - New dashboard/lib/capture-server.ts (shared by both routes): reads
    CAPTURE_SERVER_URL + CAPTURE_STREAM_SECRET from process.env with fallback
    to project-root .env.local (same file/parse as stream.ts, so OT-3's one
    secret serves both ends); derives the Bearer token with the identical
    HMAC-SHA256("capture-api") derivation (copied — dashboard can't import
    workers modules); requireLiveViewPlatform() validates exists + enabled +
    capture:"live-view"; captureServerFetch() attaches the token and maps
    connection failure to a clear "capture-server unreachable at <url>" error.
    Typed errors: CaptureConfigError (-> 503) vs CaptureRequestError (-> 400);
    other failures -> 502. Secret/token never logged, never in a response.
  - POST /api/posting-auth/connect { platform }: validates live-view, proxies
    /capture/start, returns { liveViewUrl, sessionId }; upstream error
    statuses/messages pass through (they're non-secret by US-005's contract).
  - GET /api/posting-auth/status?sessionId=…[&platform=…]: proxies
    /capture/status, returns { loggedIn }; optional platform param is
    validated live-view when present (sessions can only exist for live-view
    platforms anyway — capture-server enforces at start).
  - GET /api/posting-auth untouched.
- Files changed: dashboard/lib/capture-server.ts (new),
  dashboard/app/api/posting-auth/connect/route.ts (new),
  dashboard/app/api/posting-auth/status/route.ts (new),
  specs/live-view-browser.md
- Learnings for future iterations:
  - Runtime-verified against next dev -p 3001 in two phases: (A) no env ->
    both routes 503 with the CAPTURE_SERVER_URL message, posting-auth GET
    still 200; (B) env set + stub capture-server on 4750 that ASSERTS the
    Bearer token equals the HMAC derivation (it logged token-ok, proving the
    two independent derivations match): unknown platform 400, cli-capture
    platform 400 (via transient _verify_stub_cli config entry, backed up +
    restored, git diff clean), bad JSON/missing fields 400, stub down 502
    unreachable, happy path 200 {liveViewUrl, sessionId}, status polls
    false->true, unknown-session 404 passes through verbatim.
  - Env plumbing for next dev: pass vars on the command line
    (VAR=x npx next dev) — Next.js loads dashboard/.env.local, NOT the
    project-root .env.local; the lib's .env.local fallback covers production.
  - fuser -k 3001/tcp is also the reliable way to kill a backgrounded
    `npx next dev` (same npx-wrapper gotcha as tsx from iteration 5).
  - Operator setup for US-006 in production: put CAPTURE_SERVER_URL
    (http://127.0.0.1:4750) and CAPTURE_STREAM_SECRET in project-root
    .env.local — nothing needed in dashboard/.env.local.
  - US-007 next (final story): PostingAuthPanel Connect button — but NOTE:
    dashboard/components/PostingAuthPanel.tsx does NOT exist yet; the auth
    panel lives inside app/settings (check how /settings renders auth status
    from GET /api/posting-auth and /api/posting-platforms). Connect ->
    POST /api/posting-auth/connect, iframe sandbox="allow-same-origin
    allow-scripts", poll GET /api/posting-auth/status until loggedIn, error/
    timeout states in existing chip styling, browser-verify on /settings
    (mock connect route if OT-1/OT-2 absent). After US-007: Ship step
    (specs.md summary + readme fixes + Status: Implemented).
## Iteration 7 - US-007: "Connect" UI in PostingAuthPanel
- What was implemented:
  - Rewrote dashboard/components/PostingAuthPanel.tsx (it DID already exist,
    contrary to iteration 6's note): load() now fetches /api/posting-auth AND
    /api/posting-platforms together and derives a per-platform capture mode
    (live-view vs cli, defaulting cli). live-view platforms get a Connect /
    Re-connect (when hasSession) button; cli platforms keep the read-only
    "run capture-login" code snippet.
  - Capture state machine: idle -> connecting -> active -> idle|error.
    Connect POSTs /api/posting-auth/connect; on success mounts an
    <iframe sandbox="allow-same-origin allow-scripts"> with the liveViewUrl
    below the platform rows plus a blue "waiting for login…" chip. A useEffect
    keyed on the active phase polls GET /api/posting-auth/status every 3s
    (sessionId + platform params); loggedIn:true tears down the iframe and
    re-runs load() so "Session saved (date)" refreshes. 5-minute deadline ->
    error "Login not detected within 5 minutes". Connect/proxy/poll failures
    -> red error chip (PostingChips pill styling: rounded-full, color20 bg,
    truncated with title tooltip) inline in that platform's row; all Connect
    buttons disabled while connecting/active (single-session constraint).
- Files changed: dashboard/components/PostingAuthPanel.tsx, AGENTS.md,
  specs/live-view-browser.md
- Learnings for future iterations:
  - GOTCHA (now in AGENTS.md): Playwright must browse http://localhost:3001,
    NOT http://127.0.0.1:3001 — Next dev blocks its own script chunks as
    cross-origin from any other host, so the page serves HTML but never
    hydrates: client components sit in initial state ("Loading…") with zero
    page errors (only webpack-hmr WS console noise). Symptom looks exactly
    like a data-fetch bug.
  - Browser-verified via transient workers/posting/verify-us007.ts (deleted
    after — it would otherwise join the workers typecheck): page.route mocks
    for connect (returns stub liveViewUrl+sessionId), status (false then true),
    and the iframe URL itself (page.route intercepts subframe navigation, so
    no stub HTTP server needed). Verified: Re-connect renders for landmodo,
    iframe mounts with exact src + sandbox attrs, teardown after 2 polls,
    panel returns to idle, 503 connect -> error chip + no iframe. Screenshots
    /tmp/us007-iframe.png, /tmp/us007-error.png.
  - Both typechecks pass (dashboard npx tsc --noEmit; workers npm run
    typecheck). Real auth sessions exist for landmodo + land_com, so both
    rows show Re-connect.
  - All US-001..007 criteria are now [x]; header still "Status: Draft" —
    NEXT ITERATION IS THE SHIP STEP (specs.md summary, readme.md fixes,
    Status: Implemented (date), docs commit).
---
