# Progress Log

## Learnings
(Patterns discovered during implementation)
- Dashboard has no `typecheck` npm script — run `npx tsc --noEmit` from `dashboard/` (passes clean).
- Platform keys are canonical in `config/ad-platforms.json`; `config/posting-platforms.json` must keep the same six keys (landmodo, land_century, landflip, land_com, land_listings, landhub).
- Config JSON files use a top-level `"_doc"` string + `"platforms"` object — follow that convention for new config files.
- login_url/new_listing_url for all platforms are best-effort placeholders; verify against live sites during US-005/US-006 selector discovery.
- Port 3000 runs the PRODUCTION dashboard (`npm start` via @reboot cron) serving stale compiled code — don't test API changes against it and don't restart it. Spin up `npx next dev -p 3001` for live verification, then kill it.
- No test framework in dashboard/ — verify API routes by curling a temp dev server (create temp task → exercise → DELETE it).
- `updateTask` in dashboard/lib/data.ts merges `Partial<Task>` via spread, so any new optional Task field (like `postings`) is automatically persistable via PATCH with full-array-replace semantics — no data-layer change needed.
- Careful with `pgrep -f`: the pattern matches the checking shell's own command line; use a character class (e.g. `-p 300[1]`) to avoid self-matches.

---

## Iteration 1 - US-001: Posting data model and platform posting config
- Added `PostingStatus` type and `AdPosting` interface to dashboard/lib/types.ts; `Task` gained optional `postings?: AdPosting[]`.
- Created config/posting-platforms.json with all six platform keys (matching ad-platforms.json), each with display_name/enabled/login_url/new_listing_url; only landmodo and land_com enabled.
- Files changed: dashboard/lib/types.ts, config/posting-platforms.json
- Verified: `npx tsc --noEmit` passes; node script confirmed JSON keys match ad-platforms.json and all required fields present.
- Learnings for future iterations:
  - `postings` is optional on Task so existing tasks.json records stay valid without migration.
  - URLs in posting-platforms.json are unverified placeholders — the posting scripts (US-005/006) should treat a redirect-to-login or 404 as "verify config" territory.
---

## Iteration 2 - US-002: Task API supports posting updates and queued-posting lookup
- Added `?postable=true` filter to GET /api/tasks: returns tasks with ≥1 posting that is `queued`, or `failed` with `attempts < 3`.
- PATCH /api/tasks/[id] needed NO change — `updateTask`'s spread merge already persists `postings` with full-array-replace semantics; verified live.
- Files changed: dashboard/app/api/tasks/route.ts
- Verified: `npx tsc --noEmit` passes; live-tested on a temp `next dev -p 3001` server — queued included, failed/2 included, failed/3 excluded, posted excluded, tasks without postings excluded from postable but plain GET/PATCH unchanged. Temp tasks deleted, dev server killed.
- Learnings for future iterations:
  - The postable filter lives in the route handler (dashboard/app/api/tasks/route.ts), not lib/data.ts — US-007's run-poster.sh should call `GET /api/tasks?postable=true` and still needs to skip failed/attempts>=3 defensively per-posting when iterating (a task can be returned for one actionable posting while another posting on it is exhausted).
  - See Learnings section for prod-server-on-3000 gotcha and the temp-dev-server verification recipe.
---

## Iteration 3 - US-003: Ad output parser
- Created workers/posting/parse-ad-output.ts: `parseAdOutput(outputDir, platform)` reads `<outputDir>/<platform>.md`, extracts the `## HEADLINE (x/y chars)` and `## DESCRIPTION (x/y chars)` section bodies (stops at the next heading, so "## Why this angle" is excluded), and throws descriptive errors for missing file / missing section / empty section body.
- Created workers/posting/parse-ad-output.test.ts (node:test, 5 tests, all pass) using the real sample workers/workspace/outputs/task-1780891259218-ppunbv (landmodo + land_com), plus tmpdir fixtures for error cases.
- Created workers/posting/tsconfig.json and workers/posting/README.md.
- Files changed: workers/posting/{parse-ad-output.ts,parse-ad-output.test.ts,tsconfig.json,README.md}
- Verified: typecheck passes via dashboard's tsc; all 5 unit tests pass.
- Learnings for future iterations:
  - Node here is v20.18.1 (no TS type-stripping) and workers/ has no node_modules — workers/posting/tsconfig.json borrows the dashboard's toolchain via `typeRoots: ["../../dashboard/node_modules/@types"]` + `types: ["node"]`, and typecheck runs with `../../dashboard/node_modules/.bin/tsc -p tsconfig.json`. When US-004 runs `npm install` in workers/posting, revisit typeRoots (a local @types/node should take over).
  - tsconfig uses module CommonJS so compiled tests run directly under node; test recipe: compile with `--noEmit false --outDir /tmp/posting-test-build`, then `node --test /tmp/posting-test-build/*.test.js` FROM workers/posting/ (tests resolve the sample dir relative to cwd).
  - Only task-1779308355008-lg0z6q and task-1780891259218-ppunbv in outputs/ have the per-platform `<platform>.md` format; older dirs hold legacy single-file posts (land_post.md etc.) and some are empty — posting scripts should rely on parseAdOutput's errors rather than assuming files exist.
---
## Iteration 4 - US-004: Playwright setup and login-capture script
- Created workers/posting/package.json (playwright dep; tsx + typescript + @types/node devDeps; scripts: capture-login, typecheck, test) and ran npm install.
- Created workers/posting/capture-login.ts: reads config/posting-platforms.json, validates platform key (unknown → error listing valid keys, disabled → error listing enabled ones, no arg → usage; all exit 1), launches headed chromium on login_url, waits for operator Enter via readline, saves context.storageState to workers/posting/auth/<platform>.json (chmod 600).
- Updated README.md with setup/capture-login/typecheck/test docs; added workers/posting/auth/ to .gitignore (session files are secrets); dropped the tsconfig typeRoots hack now that local node_modules exists.
- Files changed: workers/posting/{package.json,package-lock.json,capture-login.ts,README.md,tsconfig.json}, .gitignore
- Verified: `npm run typecheck` passes; all 5 parse-ad-output tests still pass via `npm test`; all three error paths exercised live; `npx playwright install chromium` ran (Chrome Headless Shell 149 → ~/.cache/ms-playwright); headless smoke test confirmed launch + storageState save work. Full headed happy path needs an operator + display, so it remains a documented manual step.
- Learnings for future iterations:
  - workers/posting now has its own toolchain: use `npm run typecheck` and `npm test` from workers/posting/ (the old ../../dashboard/node_modules/.bin/tsc recipe is obsolete).
  - tsx runs the CommonJS TS files directly (`npm run capture-login`); no build step needed for scripts.
  - `npx playwright install chromium` on this box downloads only chromium-headless-shell; a HEADED run (capture-login, and any non-headless posting) may need the full chromium build — if `chromium.launch({headless: false})` complains about a missing executable, run `npx playwright install chromium --with-deps` or check for a display server first.
  - Scripts resolve config via path.resolve(__dirname, '../../config/...') so they work regardless of cwd; US-005/006 should follow suit.
---
## Iteration 5 - US-005: Landmodo posting script
- Created workers/posting/post-common.ts: shared plumbing for all platform posters — PosterTask/AdCopy/PostOptions/PostResult types, loadPlatformConfig, requireAuthState (throws "run capture-login"), loginExpiredError, listPhotos (photos/ dir, image extensions, sorted), splitLocation ("Elko County, Utah" → {county:"Elko", state:"Utah"}), requireListingFacts (price_usd/acreage/location from metadata), saveProofScreenshot (outputs/<taskId>/postings/<platform>.png, returns project-root-relative path), fillField (comma-separated candidate selectors, handles <select> by label→value, throws "update SELECTORS block" errors).
- Created workers/posting/post-landmodo.ts: LANDMODO_SELECTORS constants block (single-file fix per PRD) + postToLandmodo(task, adCopy, opts): auth-state load, login-page detection (URL regex + password-input selector) on landing AND after submit, form fill, photo upload if input exists, dry-run (screenshot filled form, listingUrl null), real submit waits for URL change then screenshots and returns page.url().
- Created workers/posting/post.ts CLI (`npm run post -- <platform> <taskId> [--dry-run]`): POSTERS registry (landmodo only until US-006), fetches task from DASHBOARD_URL/api/tasks/<id>, parses ad via parseAdOutput, logs to stderr, prints final JSON {listingUrl, screenshotPath} on stdout for US-007's run-poster.sh to parse.
- Created post-common.test.ts (6 tests); package.json gained "post" script; README documents the post command.
- Files changed: workers/posting/{post-common.ts,post-landmodo.ts,post.ts,post-common.test.ts,package.json,README.md}
- Verified: typecheck passes; 11/11 tests pass; CLI error paths live-tested (usage, unknown/disabled/unimplemented platform, 404 task, missing auth fail-fast with real task + real ad parse against prod API GET — read-only, safe).
- Learnings for future iterations:
  - LANDMODO_SELECTORS are UNVERIFIED Rails-style guesses — the live form needs a supervised `--dry-run` after the operator runs capture-login; expect to prune/fix that block. Same will apply to US-006.
  - Compiled test build runs from /tmp with cwd workers/posting, so post-common resolves PROJECT_ROOT via __dirname with a cwd fallback (both are <root>/workers/posting) — reuse this pattern, don't bare path.resolve(__dirname, '../..') for anything the tests import.
  - post.ts stdout contract for US-007: stderr = logs, stdout last line = JSON PostResult; exit 1 with message on stderr for any failure.
  - US-006 (land_com): add postToLandComm to the POSTERS registry in post.ts, reuse everything in post-common.ts — the platform script should only contain its SELECTORS block and flow quirks.
  - No auth/ dir exists yet on this box, so posting scripts can be exercised safely end-to-end up to the requireAuthState error without touching live sites.
---
## Iteration 6 - US-006: Land.com posting script
- Created workers/posting/post-land_com.ts: postToLandCom(task, adCopy, opts) mirroring post-landmodo.ts exactly — auth-state load, login-page detection (URL regex + password-input selector) on landing AND after submit, form fill via post-common helpers, photo upload if input exists, dry-run screenshot, real submit waits for URL change then returns page.url().
- LAND_COM_SELECTORS block uses case-insensitive name/id/placeholder attribute guesses (Land.com is a .NET/React app, not Rails) — comma-separated candidates, first match wins.
- Registered land_com in post.ts POSTERS registry; README lists implemented platforms.
- Files changed: workers/posting/{post-land_com.ts,post.ts,README.md}
- Verified: npm run typecheck passes; 11/11 tests pass; `npm run post -- land_com task-1780891259218-ppunbv --dry-run` live-tested — fetches task from prod API (read-only), parses land_com.md, then fails fast on missing auth with the capture-login hint (exit 1), same contract as landmodo.
- Learnings for future iterations:
  - LAND_COM_SELECTORS are UNVERIFIED like landmodo's — both need a supervised --dry-run after capture-login before US-007's automated runs can succeed against live sites.
  - Both posters are pure clones except the SELECTORS block and PLATFORM/SCRIPT constants; if a third platform is ever added, consider extracting the shared flow into post-common (kept duplicated for now since PRD says selectors + flow quirks stay per-script).
  - US-007's run-poster.sh can rely on: stdout last line JSON {listingUrl, screenshotPath}, stderr logs, exit 1 with human-readable message on any failure.
---
