# Progress Log

## Learnings
(Patterns discovered during implementation)
- Dashboard has no `typecheck` npm script — run `npx tsc --noEmit` from `dashboard/` (passes clean).
- Platform keys are canonical in `config/ad-platforms.json`; `config/posting-platforms.json` must keep the same six keys (landmodo, land_century, landflip, land_com, land_listings, landhub).
- Config JSON files use a top-level `"_doc"` string + `"platforms"` object — follow that convention for new config files.
- login_url/new_listing_url for all platforms are best-effort placeholders; verify against live sites during US-005/US-006 selector discovery.
- Port 3000 runs the PRODUCTION dashboard (`npm start` via @reboot cron) serving stale compiled code — don't test API changes against it and don't restart it. Spin up `npx next dev -p 3001` for live verification, then kill it.
- No test framework in dashboard/ — verify API routes by curling a temp dev server (create temp task → exercise → DELETE it).
- `updateTask` in dashboard/lib/data.ts merges `Partial<Task>` via spread, so any new optional Task field (like `postings`) is automatically persistable via PATCH with full-array-replace semantics — no data-layer change needed.
- Careful with `pgrep -f`: the pattern matches the checking shell's own command line; use a character class (e.g. `-p 300[1]`) to avoid self-matches.

---

## Iteration 1 - US-001: Posting data model and platform posting config
- Added `PostingStatus` type and `AdPosting` interface to dashboard/lib/types.ts; `Task` gained optional `postings?: AdPosting[]`.
- Created config/posting-platforms.json with all six platform keys (matching ad-platforms.json), each with display_name/enabled/login_url/new_listing_url; only landmodo and land_com enabled.
- Files changed: dashboard/lib/types.ts, config/posting-platforms.json
- Verified: `npx tsc --noEmit` passes; node script confirmed JSON keys match ad-platforms.json and all required fields present.
- Learnings for future iterations:
  - `postings` is optional on Task so existing tasks.json records stay valid without migration.
  - URLs in posting-platforms.json are unverified placeholders — the posting scripts (US-005/006) should treat a redirect-to-login or 404 as "verify config" territory.
---

## Iteration 2 - US-002: Task API supports posting updates and queued-posting lookup
- Added `?postable=true` filter to GET /api/tasks: returns tasks with ≥1 posting that is `queued`, or `failed` with `attempts < 3`.
- PATCH /api/tasks/[id] needed NO change — `updateTask`'s spread merge already persists `postings` with full-array-replace semantics; verified live.
- Files changed: dashboard/app/api/tasks/route.ts
- Verified: `npx tsc --noEmit` passes; live-tested on a temp `next dev -p 3001` server — queued included, failed/2 included, failed/3 excluded, posted excluded, tasks without postings excluded from postable but plain GET/PATCH unchanged. Temp tasks deleted, dev server killed.
- Learnings for future iterations:
  - The postable filter lives in the route handler (dashboard/app/api/tasks/route.ts), not lib/data.ts — US-007's run-poster.sh should call `GET /api/tasks?postable=true` and still needs to skip failed/attempts>=3 defensively per-posting when iterating (a task can be returned for one actionable posting while another posting on it is exhausted).
  - See Learnings section for prod-server-on-3000 gotcha and the temp-dev-server verification recipe.
---

## Iteration 3 - US-003: Ad output parser
- Created workers/posting/parse-ad-output.ts: `parseAdOutput(outputDir, platform)` reads `<outputDir>/<platform>.md`, extracts the `## HEADLINE (x/y chars)` and `## DESCRIPTION (x/y chars)` section bodies (stops at the next heading, so "## Why this angle" is excluded), and throws descriptive errors for missing file / missing section / empty section body.
- Created workers/posting/parse-ad-output.test.ts (node:test, 5 tests, all pass) using the real sample workers/workspace/outputs/task-1780891259218-ppunbv (landmodo + land_com), plus tmpdir fixtures for error cases.
- Created workers/posting/tsconfig.json and workers/posting/README.md.
- Files changed: workers/posting/{parse-ad-output.ts,parse-ad-output.test.ts,tsconfig.json,README.md}
- Verified: typecheck passes via dashboard's tsc; all 5 unit tests pass.
- Learnings for future iterations:
  - Node here is v20.18.1 (no TS type-stripping) and workers/ has no node_modules — workers/posting/tsconfig.json borrows the dashboard's toolchain via `typeRoots: ["../../dashboard/node_modules/@types"]` + `types: ["node"]`, and typecheck runs with `../../dashboard/node_modules/.bin/tsc -p tsconfig.json`. When US-004 runs `npm install` in workers/posting, revisit typeRoots (a local @types/node should take over).
  - tsconfig uses module CommonJS so compiled tests run directly under node; test recipe: compile with `--noEmit false --outDir /tmp/posting-test-build`, then `node --test /tmp/posting-test-build/*.test.js` FROM workers/posting/ (tests resolve the sample dir relative to cwd).
  - Only task-1779308355008-lg0z6q and task-1780891259218-ppunbv in outputs/ have the per-platform `<platform>.md` format; older dirs hold legacy single-file posts (land_post.md etc.) and some are empty — posting scripts should rely on parseAdOutput's errors rather than assuming files exist.
---
