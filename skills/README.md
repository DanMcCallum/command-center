# Skills

Reusable prompt fragments loaded by slash commands at runtime.

## `anti-slop.md`

Writing-quality enforcer. Catches AI-tells and weak phrasing. Loaded by `generate-ad` after the first draft, and used to self-critique + rewrite once before the output is finalized.
