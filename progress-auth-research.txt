# Progress — auth-research spike (specs/auth-research.md)

## Iteration 1 - US-001: Scaffold the report with the current-state section
- Created research/auth-capture-spike/report.md: full section skeleton (Current
  State, Land.com, Landmodo, Hosted Live-View Browsers, Other Options,
  Comparison & Recommendation, Proposed Implementation PRD) with placeholder
  notes naming which US fills each section.
- Current State section walks workers/posting/capture-login.ts (headed chromium,
  stdin Enter-prompt, storageState to workers/posting/auth/<platform>.json,
  chmod 600) and dashboard/components/PostingAuthPanel.tsx (30s poll of
  /api/posting-auth, display-only file-exists + mtime). Six pain points listed
  (PRD required 4).
- Files changed: research/auth-capture-spike/report.md (new),
  specs/auth-research.md (checked US-001), this file (new).
- Learnings for future iterations:
  - Seeded desk research at research/auth-capture-spike/initial-research.md is
    rich — it already has the vendor table, pricing, source URLs, and an initial
    recommendation. US-002..US-005 should VERIFY and cite it, not re-research.
    Its "unverified" items are enumerated near the bottom of that file.
  - Both typechecks pass clean as of this iteration: `cd workers/posting && npm
    run typecheck` and `cd dashboard && npx tsc --noEmit`. Report is
    markdown-only, so typecheck is just a no-regression gate.
  - US-008 wants a regeneration command noted at the top of report.md — don't
    add it earlier, it's that story's criterion.
  - Playwright/Chromium already installed in workers/posting — reuse for US-003
    recon and US-008 PDF render. Note from feature log: Playwright scripts must
    live INSIDE workers/posting/ (ESM resolves playwright relative to script
    file).
  - workers/posting/auth/*.json are secrets — never quote their contents in the
    report, paths only.
---

## Iteration 2 - US-002: Verify the Land.com LandFeed API
- Verified the LandFeed XML API firsthand and filled in §2 of report.md.
- Fetch results (all 2026-07-10): landsofamerica.com/LandFeed/Docs/ = HTTP 500
  (still broken, as seeded research said); land.com/LandFeed/Docs/ = 200 (same
  page on the other host); Wayback snapshot 20240423062829 = 200 and returned the
  FULL spec (LandFeed API Specification v2.1, Sept 21 2022); the XSD
  land.com/LandFeed/schemas/LandFeedSchema1.0.xsd = 200; /LandFeed/states/ = 200;
  POST endpoint land.com/LandFeed/ = 200.
- Wrote: what the feed does (INSERT/UPDATE/DELETE by customer listing id; feed is
  authoritative — omitting a listing DELETES it; send everything every time; test
  mode exists), auth model (creds live IN the XML: loa_account_id +
  loa_account_email + loa_shared_key, shared key issued by Land.com staff;
  requires active Corporate Account; TLS 1.2+, Content-Type text/xml, User-Agent),
  a key-fields schema table, latency (listings 5min-1hr, images 10min-6hr), cost
  ($0 beyond existing plan), 6 open questions, a ready-to-send draft support
  email, and the fallback (keep Playwright poster for land_com, capture via the
  Landmodo approach in §4).
- Files changed: research/auth-capture-spike/report.md (§2 filled),
  specs/auth-research.md (US-002 checked), this file.
- Learnings for future iterations:
  - WebFetch CANNOT reach web.archive.org (returns "unable to fetch"). Use curl to
    pull Wayback snapshots, then sed 's/<[^>]*>/ /g' to strip tags. The Wayback
    availability API (archive.org/wayback/available?url=...) finds the closest
    snapshot fast.
  - WebFetch also refuses HTTP 500 pages (no body). curl -o file still saves
    whatever the server returns; for a 500 there's no body, so pivot to archive.
  - land.com and landsofamerica.com are the SAME site (CoStar Group); when one
    host errors, try the other before giving up.
  - For US-004 (vendor pricing) WebFetch should work on live vendor docs pages —
    only archive.org and 500s are blocked. Record source URL + fetch date per the
    PRD's "web is live and changes" rule.
  - Both typechecks still pass clean (posting exit 0, dashboard tsc exit 0);
    report is markdown-only so it's purely a no-regression gate.
---

## Iteration 3 - US-003: Recon the Landmodo login flow
- Wrote workers/posting/recon-landmodo.ts (headless, default-fingerprint Chromium
  = the poster's install). It loads landmodo.com/login, full-page screenshots to
  research/auth-capture-spike/artifacts/landmodo-login.png, scans HTML for known
  bot-wall/CAPTCHA vendor signatures, and dumps the login form fields. Recon only:
  never types credentials, never submits.
- Findings (all 2026-07-10): login HTTP 200, renders fully for a headless
  datacenter browser (no Cloudflare/bot wall). Only bot defense = Google
  reCAPTCHA (g-recaptcha container + hidden `recaptcha` input in form
  member_login_190) rendered INVISIBLY — screenshot shows a plain email/password
  card, no checkbox/challenge. No hCaptcha/Turnstile/DataDome/PerimeterX/Arkose.
  No MFA/OTP markers. Login form: email (name=email), pass (name=pass), hidden
  recaptcha token, "Login Now" submit + hidden plumbing fields.
- API absence confirmed from landmodo.com/seller-support (HTTP 200): "How do I
  post a Property?" = manual dashboard flow only; no api/bulk/csv/import/feed/xml
  in visible text. how-it-works + list-your-company = 404. Noted the email-support
  action as an operator task.
- Filled §3 of report.md (verdict, evidence table, load analysis, bot markers,
  form table, API-absence, sources). Implication recorded: credential-vault
  scripted login is riskiest (invisible reCAPTCHA scores down datacenter logins);
  hosted live-view (§4) is the clean fit (human passes reCAPTCHA, we persist).
- Files changed: workers/posting/recon-landmodo.ts (new),
  research/auth-capture-spike/report.md (§3), artifacts/landmodo-login.png (new),
  specs/auth-research.md (US-003 checked), this file.
- Learnings for future iterations:
  - The recon script lives in workers/posting/ so tsconfig include ["*.ts"] pulls
    it into `npm run typecheck` — it must stay type-clean (it does, exit 0).
  - Playwright gotcha: waitUntil 'networkidle' TIMED OUT on landmodo.com/seller-support
    (long-polling/analytics keep the network busy). Use waitUntil 'domcontentloaded'
    + a short page.waitForTimeout(2500) for content pages; 'networkidle' was fine on
    the lighter /login page. For the screenshot I used networkidle with a .catch()
    fallback so a busy network never aborts the shot.
  - Keyword-in-HTML is a false-positive trap: api/feed/xml/import all appeared in
    raw HTML of 404 pages (script/meta chrome). Verify against body.innerText
    (visible text) and cross-check that the same hit isn't on an unrelated 404.
  - reCAPTCHA version was ambiguous from markers (a g-recaptcha div present but no
    data-sitekey / grecaptcha.execute) — the honest, screenshot-grounded statement
    is "present but renders no visible challenge on the login tab," which is what
    matters for the proposal. Don't overclaim v2 vs v3.
  - Both typechecks still pass clean (posting exit 0, dashboard tsc exit 0).
---

## Iteration 4 - US-004: Evaluate hosted live-view browser vendors
- Filled §4 of report.md with verified 2026-07-10 data for Browserbase, Steel.dev,
  Anchor Browser, Hyperbrowser: pricing + free tiers, live-view iframe embed
  mechanics, profile/context persistence, and how state returns to our workers
  (connectOverCDP vs CDP Network.getAllCookies cookie export to storageState).
  Added cost projections at 1 user (~0.6 br-hr/mo) and 20 users (~31 br-hr/mo)
  from the PRD's 2-min-capture / 3-min-post assumptions, a per-vendor cost table,
  an integration sketch (Connect button -> POST /api/posting-auth/connect ->
  persistent session -> iframe -> poll for login -> persist), and the exact list
  of files a follow-up PRD would touch.
- Files changed: research/auth-capture-spike/report.md (§4 filled),
  specs/auth-research.md (US-004 checked), this file.
- Learnings for future iterations:
  - PRICING DRIFT is real and material: Steel.dev's cheap ~$29/mo tier from the
    seeded research is GONE. steel.dev/pricing now shows Launch (free, $30 ONE-TIME
    credits) then a $250/mo Scale tier - no cheap recurring plan. The seeded
    research's recommendation leaned on Steel as the "value/open-source pick"; that
    now means self-host or pay $250/mo. US-006 recommendation must reflect this:
    Browserbase Developer $20/mo is now the cheapest turnkey recurring option at
    20 users (and bundles CAPTCHA solving, which matters given Landmodo's invisible
    reCAPTCHA from §3). Flag this drift explicitly in the comparison.
  - WebFetch on JS-rendered marketing pages returns ONLY the <title> (Hyperbrowser
    /pricing and steel had partial issues). Fallbacks that worked: (a) WebSearch
    the pricing (Hyperbrowser credit model surfaced cleanly: 1 credit=$0.001, 1
    br-hr=100 credits=$0.10, free 1000 credits/1 concurrent, Startup $30/mo); (b)
    fetch the DOCS pricing page instead of the marketing one (docs.* pages are
    static markdown and fetch clean - Browserbase/Anchor/Steel docs all worked).
  - Redirect handling: docs.hyperbrowser.ai 308-redirects to hyperbrowser.ai/docs/*.
    WebFetch returns the redirect target rather than following it - just re-call
    with the given URL.
  - None of the four vendors expose a reliable "user logged in" event - the
    integration must POLL session state (auth cookie / redirect off /login) to
    detect login success. Recorded this as a design constraint for US-007's PRD.
  - Cookie-export-to-storageState (CDP Network.getAllCookies -> auth/<platform>.json)
    is the LOW-RISK integration path: post.ts and the poster pipeline stay
    unchanged. connectOverCDP-against-vendor-profile is the alternative but rewrites
    the poster's auth load. US-007 should default to the cookie-export path.
  - Both typechecks still pass clean (posting exit 0, dashboard tsc exit 0); report
    is markdown-only so it's a no-regression gate.
---

## Iteration 5 - US-005: Evaluate the remaining options
- Filled §5 (Other Options) of report.md: (a) browser-extension cookie export,
  (b) Apify, (c) self-hosted neko/noVNC/CDP screencast, (d) credential vault.
  Each on the identical rubric (UX flow / build effort / reliability-fragility /
  security posture / cost) with an explicit verdict line, plus a summary verdict
  table at the end. Cross-referenced §3's invisible-reCAPTCHA finding into (a),
  (c), and (d) so the fragility claims are grounded, not guessed.
- Verdicts: (a) viable fallback/break-glass; (b) rejected for scale (solves worker
  hosting we already have, no better capture UX than (a)); (c) viable fallback but
  deferred (1-2 wk + ops vs 1-2 days for identical UX); (d) REJECTED on the
  no-password-custody constraint (required by AC), noted it'd also be most fragile
  vs Landmodo's invisible reCAPTCHA.
- Files changed: research/auth-capture-spike/report.md (§5 filled),
  specs/auth-research.md (US-005 checked), this file.
- Learnings for future iterations:
  - Live-verified Apify pricing via WebFetch on apify.com/pricing (works clean, not
    JS-blocked like the §4 vendor marketing pages): Free $0/$5cr, Starter $29/$29cr,
    Scale $199, Business $999; CU $0.20 (Scale $0.16/Business $0.13); residential
    proxy $8/GB. Seeded research's Apify figures ($29 Starter, ~$0.20/CU, $8/GB) all
    still current — no drift, unlike Steel.dev in §4.
  - US-006 (comparison matrix) can now pull every option's verdict + $/mo straight
    from the §5 summary table and §4's per-vendor cost table — no new research
    needed, it's a synthesis story. Recommendation should be: land_com -> LandFeed
    API (§2, pending eligibility); landmodo -> hosted live-view Browserbase (§4);
    fallbacks -> extension export (5a) or self-host neko (5c); reject vault (5d) +
    Apify (5b).
  - Rubric-per-option + explicit verdict line + a closing summary table is a clean
    pattern for these "evaluate N alternatives" sections; US-006 mirrors it at the
    cross-option level.
  - Both typechecks still pass clean (posting exit 0, dashboard tsc exit 0); report
    is markdown-only so it's a no-regression gate.
---

## Iteration 6 - US-006: Comparison matrix and recommendation
- Filled §6 (Comparison & Recommendation) of report.md by synthesizing §2-§5 — no
  new research. A 10-row matrix (current-CLI baseline + LandFeed + 4 live-view
  vendors + 4 other options) scored UX/effort/reliability/security with ▲/●/▼
  glyphs plus $/mo now and $/mo @20 users pulled from §4's cost table and §5's
  per-option costs. Then: per-platform recommendation (land_com -> LandFeed API
  pending eligibility, fallback live-view; landmodo -> Browserbase live-view,
  fallbacks extension export then self-hosted neko), a 6-item "what would change
  this decision" list, and an 8-item explicit UX/reliability-gains-over-CLI list.
- Files changed: research/auth-capture-spike/report.md (§6 filled),
  specs/auth-research.md (US-006 checked), this file.
- Learnings for future iterations:
  - US-006 was pure synthesis — every number/verdict already existed in §2-§5, so
    the matrix just cites section numbers (§N) rather than re-deriving. US-007
    (draft the follow-up PRD) is likewise a synthesis story: it should turn the §6
    recommendation into Ralph-sized US-XXX stories (config/schema -> backend -> UI),
    flag operator-task prerequisites (the §2 Land.com support email + the §3
    Landmodo support email are NOT Ralph stories), and require "Typecheck passes"
    (+ browser verification for UI stories) on each.
  - The recommended per-platform approaches US-007 must PRD out: (1) land_com
    LandFeed XML feed builder + public photo hosting, gated on the operator getting
    a shared key; (2) landmodo Browserbase live-view capture — new
    POST /api/posting-auth/connect route + iframe in PostingAuthPanel.tsx + poll for
    login success + CDP cookie export to auth/landmodo.json (the low-risk path that
    keeps post.ts unchanged, per §4's integration sketch "files that would change").
  - Matrix glyph legend (▲ good / ● middling / ▼ poor for our constraints) is
    defined inline right before the table — keep any US-007/US-008 table that reuses
    glyphs self-describing so the PDF render (US-008) is legible standalone.
  - Both typechecks still pass clean (posting exit 0, dashboard tsc exit 0); report
    is markdown-only so it's a no-regression gate.
---

## Iteration 7 - US-007: Draft the follow-up implementation PRD inside the proposal
- Filled §7 (Proposed Implementation PRD) of report.md — pure synthesis of §6's
  per-platform recommendation, no new research. Wrote Introduction / Goals /
  Non-Goals, then Ralph-sized US-XXX stories in config/schema -> backend -> UI order.
  Land.com LandFeed path: US-101 (config + LOA_SHARED_KEY env), US-102 (build
  schema-valid XML feed from tasks), US-103 (public photo URLs), US-104 (submit in
  test mode + record via PATCH). Landmodo Browserbase path: US-201 (live-view
  capture config), US-202 (POST /api/posting-auth/connect + status poll + CDP
  cookie export to auth/landmodo.json), US-203 (Connect button + iframe in
  PostingAuthPanel.tsx). Each story ends in "Typecheck passes"; the UI story
  (US-203) also requires browser verification on port 3001.
- Human prerequisites flagged separately as Operator Tasks (NOT Ralph stories):
  OT-1 email Land.com for shared key (gates US-104 live submit), OT-2 create
  Browserbase account + API key (gates US-202/203 live capture), OT-3 decide public
  photo hosting (informs US-103). Added a story-sequencing block making the two
  platforms independent with gates noted.
- Files changed: research/auth-capture-spike/report.md (§7 filled),
  specs/auth-research.md (US-007 checked), this file.
- Learnings for future iterations:
  - US-008 is the last story: render report.md -> auth-capture-proposal.pdf. Reuse
    workers/posting Playwright/Chromium print-to-PDF (page.pdf) OR pandoc if
    installed — NO new paid services. PDF must start with %PDF, be >20KB, >=5 pages
    (verify with pdfinfo). Tables (§4 vendor pricing, §6 comparison matrix) must
    render legibly — convert a page to image or open in browser to check. Note a
    regeneration command at the TOP of report.md (that's a US-008 criterion — the
    top of report.md currently has NO regen command, so add it in US-008).
  - The report is now content-complete through §7; only the PDF render + the
    top-of-file regen note remain. After US-008 all ACs are [x] and the SHIP step
    kicks in (add specs.md feature-log summary, fix readme.md, flip header to
    Implemented). Ship is a SEPARATE final iteration per the Ralph instructions.
  - Playwright page.pdf needs headless chromium + a file:// or data URL of rendered
    HTML. Markdown->HTML: either pandoc, or a tiny marked/markdown-it render inside
    workers/posting (it already has npm deps). Keep any such script INSIDE
    workers/posting so it stays in `npm run typecheck` and ESM resolves deps.
  - Both typechecks still pass clean (posting exit 0, dashboard tsc exit 0); report
    is markdown-only so it's a no-regression gate.
---

## Iteration 8 - US-008: Render the proposal as a PDF
- Wrote workers/posting/render-pdf.ts: reads research/auth-capture-spike/report.md,
  converts markdown -> HTML with a hand-rolled parser (no new dependency — keeps
  the posting typecheck green since a bare `marked` import would be untyped), and
  prints to research/auth-capture-spike/auth-capture-proposal.pdf via the existing
  Playwright/Chromium (page.setContent -> page.pdf, Letter, printBackground). Added
  a regeneration note (blockquote) to the TOP of report.md:
  `cd workers/posting && npx tsx render-pdf.ts`.
- Verified the PDF: starts with %PDF-1.4, 281 KB (>20 KB), 27 pages (page tree
  /Kids = 8+8+8+3 leaf pages; MediaBox count = 27) >> the 5-page floor. Rendered
  the comparison matrix + vendor pricing tables to PNGs and eyeballed them — glyphs
  (▲/●/▼), bold, and inline code all legible; even-row striping + bordered cells.
- Files changed: workers/posting/render-pdf.ts (new), report.md (regen note),
  specs/auth-research.md (US-008 checked), auth-capture-proposal.pdf (new), this file.
- Learnings for future iterations:
  - The hand-rolled mdToHtml covers exactly what report.md uses: h1-h6, GFM pipe
    tables (header + `|---|` separator), ul/ol, blockquotes, hr, paragraphs, and
    inline bold/`code`/[links]. No code fences or images in the report. Inline code
    is protected BEFORE html-escaping so `<`/`&` inside backticks survive; bold and
    links run after escape. Single-`*` italic is intentionally NOT handled (the
    matrix glyphs and `*` in prose would get mangled) — add it only if a future
    report needs it.
  - render-pdf.ts guards main() behind `if (require.main === module)` and EXPORTS
    mdToHtml + CSS, so a verification script can import them and screenshot the HTML
    without re-rendering the PDF. Same ESM gotcha as the posting scripts: any such
    helper MUST live inside workers/posting or `playwright`/`./render-pdf` won't
    resolve (I hit MODULE_NOT_FOUND running it from /tmp).
  - Chromium's native PDF viewer will NOT screenshot from a `data:application/pdf`
    URL in headless (navigation errors). To verify table legibility, screenshot the
    rendered HTML (locator('table').screenshot()) instead — same content, reliable.
  - No pandoc / pdfinfo / wkhtmltopdf on this box; Playwright print-to-PDF is the
    only local render path. Page count without pdfinfo: grep the page-tree /Kids
    arrays or count MediaBox occurrences.
  - ALL ACs in auth-research.md are now [x] but the header still says Draft, so the
    NEXT iteration is the SHIP step (specs.md feature-log summary + readme.md fixes
    + flip header to Implemented (2026-07-10)), per the Ralph instructions.
  - Both typechecks pass (posting exit 0 incl. render-pdf.ts, dashboard tsc exit 0).
---
