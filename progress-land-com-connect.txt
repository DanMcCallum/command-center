# Progress Log — Land.com Access Research Spike (specs/land-com-connect.md)

## Learnings
(Patterns discovered during implementation)

- Journal evidence: `journalctl --user -u capture-server` is noisy with `key-debug`
  lines — filter with `grep -vE 'key-debug|typed [0-9]+ chars'` to see the capture
  lifecycle. The three false positives are at 04:50:55/04:51:04/04:51:11 UTC.
- `workers/posting/capture/detect.ts` has an UNCOMMITTED partial mitigation in the
  working tree (`sawLoginPage` guard). Do not commit it with spike stories, and don't
  describe it as shipped — the report's finding section covers why it's insufficient
  (the Access Denied flow does touch /login first).
- The bogus `auth/land_com.json` was already deleted (~04:57); only `landmodo.json`
  exists. US-007 will create land_com.json fresh from the OT-B export.
- The branch carries many unrelated modified files — stage spike files explicitly
  by path, never `git add -A`.
- `git check-ignore -v` + a throwaway touch file is a quick way to prove a scoped
  .gitignore (`*` + `!README.md` + `!.gitignore`) does what the AC asks.

---

## Iteration 1 - US-001: Scaffold the report and document the block with evidence
- Created research/land-com-connect-spike/report.md with all seven required section
  headings; The Block section has fresh worker-box curl probes (both 403 at
  2026-07-11T05:54:34Z), the verbatim Akamai/edgesuite Access Denied page pattern,
  the 04:50–05:12 UTC journal timeline (three ~2s false "capture succeeded" runs
  with session ids), and the redirect_off false-positive finding (mechanism, root
  cause, follow-up flag, detect.ts reference). Remaining sections are stubs naming
  the story that fills them.
- Created operator-input/ with a scoped .gitignore (everything ignored except
  .gitignore + README.md) and a README explaining the OT-A (home-probes.txt) and
  OT-B (cookie export) drop-offs plus safety rules.
- Files changed: research/land-com-connect-spike/report.md,
  research/land-com-connect-spike/operator-input/{.gitignore,README.md},
  specs/land-com-connect.md (US-001 checked off)
- Learnings: see Learnings section above (journal filtering, uncommitted detect.ts
  guard, explicit staging).
---

## Iteration 2 - US-002: Map the block scope from the worker box and prepare home probes
- Probed all seven URLs from the worker box (2026-07-11T05:58Z): www.land.com/
  and /login 403 (~370 B Akamai denial), all three LandFeed endpoints 200 with
  real content (XSD serves actual schema XML), and both sister brands
  (landsofamerica.com, landwatch.com) 403. Filled the report's Block Scope
  section: probe table (home column "awaiting OT-A"), first-line 200
  classification, explicit conclusion (LandFeed EXEMPT from the block — the
  sanctioned API route survives; block covers the whole consumer surface incl.
  sister brands, so no sister-site login loophole).
- Wrote research/land-com-connect-spike/home-probe.sh (plain POSIX curl loop,
  prints no bodies, chmod +x) and ran it end-to-end; updated operator-input
  README to drop the "arrives with US-002" placeholder.
- Files changed: research/land-com-connect-spike/report.md,
  research/land-com-connect-spike/home-probe.sh (new),
  research/land-com-connect-spike/operator-input/README.md,
  specs/land-com-connect.md (US-002 checked off)
- Learnings for future iterations:
  - The Akamai denial body is ~366–376 bytes — response size alone cleanly
    separates denial pages from real content in probe output.
  - LandFeed 200s: the two HTML pages are JS-shell documents (empty <title>),
    so "first line is <!DOCTYPE html> and size ≫ 400 B" is the classification,
    not the title; the .xsd is directly verifiable (<?xml + <xs:schema).
  - US-006 can now state plainly that the feed endpoint is reachable from the
    box — the block does NOT change LandFeed viability.
---

## Iteration 3 - US-003: Research the capture-from-user's-browser family
- Replaced the "Capture From the User's Browser" stub with the full section:
  the HttpOnly-decides-everything framing (chrome.cookies/DevTools/CDP see
  HttpOnly, document.cookie/CookieStore.getAll do not — both verified with
  fetch-dated sources); sub-options (a) manual DevTools/cookie-export-extension
  paste [Recommended for spike], (b) minimal MV3 extension POSTing cookies
  [viable fallback / future upgrade — production code, out of scope], (c)
  bookmarklet [Rejected — impossible for HttpOnly session cookies]; the full
  cookie→Playwright storageState conversion shape with per-attribute preservation
  notes (domain leading-dot, expires in seconds/-1, sameSite normalization map,
  localStorage caveat that chrome.cookies can't read it); and the replay caveat
  with three explicit pairings (capture+residential-egress, capture+run-locally,
  capture+worker-box=still-403).
- Files changed: research/land-com-connect-spike/report.md,
  specs/land-com-connect.md (US-003 checked off), progress-land-com-connect.txt
- Learnings for future iterations:
  - The load-bearing fact for the whole browser-capture family: HttpOnly session
    cookies are readable ONLY via chrome.cookies (extension), DevTools, or CDP
    Network.getAllCookies — never via page JS. This is why the bookmarklet path
    is a dead end and why US-004's export instructions must use DevTools or a
    cookie-export extension, not a bookmarklet.
  - Verified Playwright storageState shape for US-007's converter:
    {cookies:[{name,value,domain,path,expires,httpOnly,secure,sameSite}],
    origins:[{origin,localStorage:[{name,value}]}]}. sameSite must be normalized
    to Strict|Lax|None (exporters emit no_restriction/unspecified) or Playwright
    rejects the file; expires is seconds (-1 for session cookies).
  - US-003 is doc-only, so typecheck is a no-op pass — it touches no .ts. Same
    will be true for US-004/US-005/US-006/US-008; only US-007 adds a script.
  - The replay caveat is the through-line for US-005 and US-008: capture-from-
    browser fixes capture but NOT posting from the box; it must be paired with an
    egress option, or superseded by LandFeed (US-006, endpoint not blocked).
---

## Iteration 4 - US-004: Write the operator cookie-export instructions (OT-B enabler)
- Rewrote operator-input/README.md's OT-B section from a placeholder into full
  numbered instructions for the US-003-recommended method (cookie-export
  extension): Chrome/Chromium (or Firefox) with Cookie-Editor, log in on
  www.land.com, export ALL site cookies as JSON (both www.land.com host cookies
  and domain-wide .land.com, HttpOnly included), save exactly as
  operator-input/land_com-cookies.json, plus a shape sanity-check step
  (must contain at least one "httpOnly": true entry — proves the export ran with
  extension privileges) and the standing safety rules (gitignored dir only,
  deleted after conversion, values never in report/logs/chat, log-out-to-revoke
  if leaked).
- Report's Capture From the User's Browser section (sub-option (a)) now links to
  the instructions and records that OT-B is actionable.
- Files changed: research/land-com-connect-spike/operator-input/README.md,
  research/land-com-connect-spike/report.md,
  specs/land-com-connect.md (US-004 checked off)
- Learnings for future iterations:
  - Instructions deliberately defer the localStorage question: cookies-only export
    first; if US-007 validation shows cookies alone don't authenticate, US-007
    adds a DevTools localStorage step then. Don't front-load operator friction.
  - The exact drop-off filename for US-007's converter is
    operator-input/land_com-cookies.json (Cookie-Editor JSON array shape:
    name/value/domain/path/expirationDate/httpOnly/secure/sameSite; sameSite may
    be no_restriction/unspecified — converter must normalize, expirationDate is
    float seconds and absent for session cookies).
  - Typecheck is still a no-op pass for doc-only stories (tsc exits clean).
---

## Iteration 5 - US-005: Research residential egress and run-locally families
- Replaced the "Residential Egress" and "Run Locally" stubs with full sections.
  Residential Egress: framing (IP-reputation block → egress fixes capture AND
  posting), three options each with a 5-dimension scoring table (setup effort,
  ops burden on home-box reboot, fragility, security exposure, $/mo) —
  (a) WireGuard/SSH SOCKS tunnel ($0), (b) Tailscale exit node ($0 Personal free
  tier, exit nodes included; flagged that the paid Mullvad add-on is the WRONG
  path since it reintroduces datacenter IPs), (c) commercial residential proxy
  (IPRoyal $1.75/GB PAYG, market $1.75–8/GB). Attach-point subsection: Playwright
  `proxy` option + Chromium `--proxy-server` config sketch (illustrative, not
  committed), noting Tailscale/WG can be route-level with no browser change, and
  the proxy credential is a secret like auth/*.json. Verdicts against the US-003
  rubric: Tailscale Recommended, hand-rolled tunnel viable fallback, commercial
  proxy last resort (trust cost of routing an authed session through a 3rd party).
  Run Locally: two granularities (capture-only-local + sync land_com.json;
  whole-poster-local), each with the honest caveat that capture-only-local still
  403s from the box unless paired with egress, and verdicts.
- Files changed: research/land-com-connect-spike/report.md,
  specs/land-com-connect.md (US-005 checked off), progress-land-com-connect.txt
- Learnings for future iterations:
  - Pricing anchored 2026-07-11: Tailscale Personal = free forever, up to 6 users
    / unlimited devices, exit nodes INCLUDED (tailscale.com/pricing). IPRoyal
    residential from $1.75/GB PAYG (iproyal.com/pricing); 2026 market tiers
    ~$1.75 (budget) / ~$3–4 (Decodo mid) / ~$8 (Bright Data/Oxylabs entry)
    (aimultiple.com/proxy-pricing). Reuse these in US-008's matrix ($/mo column).
  - The through-line for US-008: residential egress fixes BOTH capture and posting
    (unlike capture-from-browser which fixes only capture, and unlike LandFeed
    which sidesteps both). Tailscale exit node is the recommended egress; the
    posting-egress follow-up should default to it.
  - US-005 is doc-only — typecheck (cd workers/posting && npm run typecheck) is a
    clean no-op pass; it touches no .ts. Same holds for US-006/US-008.

## Iteration 6 - US-006: Re-anchor the LandFeed API option
- Replaced the "LandFeed API" stub with the full section. Summarizes (not
  re-researches) the prior spike's §2 verified findings: auth model (creds inside
  the XML <channel> — loa_account_id/email/shared_key, no OAuth/cookie/session,
  never expires), full-inventory-diff gotcha (one POST carries the ENTIRE
  inventory; absent listing ID = DELETE; mode=test validates without touching
  live), photo-by-URL requirement (image_link/loa_photo_tour_images pulled from
  public URLs ≤2MB — diverges from the local-file poster), and the Corporate
  Account prerequisite. Incorporates the US-002 probe result (all three LandFeed
  endpoints 200 from the box on 2026-07-11, matching 2026-07-10) and states
  plainly: the block does NOT change feed viability — /LandFeed/ is exempt, so the
  box can POST the feed with zero egress. Restates OT-C (shared-key email, target
  support@land.com) with status "not yet reported by the operator" and names the
  fallback chain if feed access is denied (browser-capture+Tailscale egress →
  run-locally → commercial proxy).
- Files changed: research/land-com-connect-spike/report.md,
  specs/land-com-connect.md (US-006 checked off), progress-land-com-connect.txt
- Learnings for future iterations:
  - The load-bearing fact for US-008's matrix: LandFeed is the ONLY option that
    fixes both capture and posting at $0 with no egress (endpoint exempt from the
    block), but it's gated on OT-C eligibility. When eligible it dominates; when
    denied the fallback is browser-posting + egress. US-008 should rank it first
    with the eligibility caveat.
  - OT-C status is still unreported — US-008's "what would change this decision"
    list should include "LandFeed shared key granted" as the top entry.
  - Doc-only story; typecheck (cd workers/posting && npm run typecheck) is a clean
    no-op pass. Only US-007 remains as a code-touching story (the converter script).
---

## Iteration 7 - US-007: Capture and validate a real land.com session
- OT-B export (operator-input/land_com-cookies.json) was ABSENT, so the story
  completed via its explicit absent-input path: added a "Session Capture &
  Validation (US-007)" section to the report recording the blocked status
  (checked 2026-07-11T06:14:27Z), the exact OT-B ask (with README link), and
  the ready-to-run validation plan. No session captured, no auth/land_com.json
  written, no validation fabricated.
- Built the converter anyway (the story's non-gated half):
  workers/posting/research-convert-cookies.ts — throwaway, research-* named,
  in-package, wired into nothing. Reads the OT-B drop-off by default (or
  argv[2]/argv[3]), accepts raw Cookie-Editor arrays or {cookies:[...]}
  wrappers, filters to land.com domains, normalizes sameSite
  (no_restriction→None, unspecified/absent→Lax), expirationDate float-secs →
  integer expires (-1 for session cookies), writes storageState chmod 600,
  fails closed on missing input / empty array / zero HttpOnly cookies (the
  page-script-export signature), prints names/flags/expiries only — never values.
- Mechanics verified against a SYNTHETIC fixture in /tmp (fake values, deleted
  after): output loads into a Playwright chromium context (3 cookies), both
  guards exit 1 with actionable messages. Also linked the converter command from
  the operator-input README step 7.
- Files changed: workers/posting/research-convert-cookies.ts (new),
  research/land-com-connect-spike/report.md,
  research/land-com-connect-spike/operator-input/README.md,
  specs/land-com-connect.md (US-007 checked off, with an OT-B-absent-path note),
  AGENTS.md (new bullet), progress-land-com-connect.txt
- Learnings for future iterations:
  - workers/posting tsconfig is module:CommonJS — import.meta is a typecheck
    error; use __dirname like capture-login.ts does.
  - PRE-EXISTING test failure on this branch, NOT from the spike: npm test in
    workers/posting fails 1/20 ("parses headline and description from a real
    landmodo sample") because parse-ad-output.test.ts asserts against the LIVE
    workspace file outputs/task-1780891259218-ppunbv/landmodo.md, which carries
    an uncommitted edit appending "(Property: Desert Basecamp)" — the asserted
    final sentence no longer ends the description. Predates this iteration
    (file listed dirty at branch start); typecheck passes and US-007's script
    is not exercised by the suite. Added an AGENTS.md bullet about this class
    of failure. Whoever commits/reverts those workspace edits should reconcile
    the test.
  - US-008 (last story) is doc-only. After it, the Ship iteration remains.
    When OT-B lands, the validation plan is in the report's US-007 section —
    converter is done, only the run itself is pending.
---
