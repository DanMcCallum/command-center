# Task: Move Roadmap to its own section

## Changes made

1. **Created** `/dashboard/app/roadmap/page.tsx` — full dedicated Roadmap page that fetches from `/api/todos`, renders items with expandable body text, same styling as the rest of the app.

2. **Updated** `/dashboard/components/Nav.tsx` — added `{ href: '/roadmap', label: 'Roadmap' }` to the ITEMS array so it appears in the top nav.

3. **Updated** `/dashboard/app/tasks/page.tsx` — removed `RoadmapSection` import and its usage so roadmap no longer appears inline on the Tasks page.

The `RoadmapSection` component itself was left in place (not deleted) since it could still be useful elsewhere.
