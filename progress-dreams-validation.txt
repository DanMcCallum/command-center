# Progress — DREAMS Review Loop in Ad Generation (specs/dreams-validation.md)

## Iteration 1 - US-001: DREAMS audit step in the generate-ad workflow
- What was implemented:
  - Added input item 9 to `~/.claude/commands/generate-ad.md`: the DREAMS rubric at
    `/home/mazer/claude/command-center/.claude/skills/dreams-ad-review/SKILL.md`, read
    directly as a rubric file (headless — explicit "never invoke as a skill" instruction),
    to be read in full before step 6.
  - Added `### Step 6 — DREAMS audit` after Step 5 (voice check), before `## Output`:
    audits every platform variant's headline + description sales-copy portion (nickname
    tag explicitly exempt, same as steps 4/5) against all six DREAMS categories; one
    scorecard pass per cycle covering all platforms (no per-variant sub-loops, per the
    PRD's timeout-budget consideration).
  - Specified audit output format: per platform, per category, verdict `Pass | Weak | Missing`
    plus a 1–2 sentence coaching note for every non-Pass verdict; each cycle's full audit
    appended to `notes.md` with cycle number, per-platform scorecard table, and coaching notes.
- Files changed:
  - `~/.claude/commands/generate-ad.md` (OUTSIDE the repo — not git-tracked!)
  - `specs/dreams-validation.md` (checked off US-001 criteria)
  - Backup taken first: `workers/workspace/notes/generate-ad.md.bak-2026-07-10`
    (pre-edit copy of the workflow file, per PRD Technical Considerations)
- Verification: `npx tsc --noEmit` from `dashboard/` PASSED; grep confirmed all required
  sections/phrases are present in the workflow file.
- Learnings for future iterations:
  - The workflow file lives at `~/.claude/commands/generate-ad.md`, outside the repo.
    The pre-edit backup already exists at `workers/workspace/notes/generate-ad.md.bak-2026-07-10`
    — do NOT overwrite it on later iterations; it should stay the pristine pre-feature copy.
  - Step numbering after the edit: Step 4a/4b (headline-craft + anti-slop), Step 5 (voice),
    Step 6 (DREAMS audit, currently audit+logging only). US-002 extends Step 6 with the
    revise→re-audit loop, termination (3 cycles), and precedence rules, and must update
    step 4b's "exactly once ... After one pass, the variant is final" wording, plus step 3's
    "only append the tag as the literal last thing ... after every rewrite pass" already
    reads compatibly (tag after ALL passes) but re-check it when adding the loop.
  - notes.md requirements in the `## Output` section were deliberately NOT touched —
    that's US-003's criterion ("notes.md requirements updated to include the per-cycle
    audit logs"). The logging requirement currently lives inside Step 6 itself.
  - Repo working tree has unrelated dirty files from the ad-posting branch work
    (dashboard/.next-start.log, data/tasks.json, progress.txt, specs/ad-posting.md,
    specs/readme.md, specs/specs.md untracked-modified). Stage only your own files.
---
## Iteration 2 - US-002: Revise-and-repeat loop with termination and precedence rules
- What was implemented (all in ~/.claude/commands/generate-ad.md):
  - Extended Step 6 with four new blocks after the cycle-log format:
    **Revision pass** (rewrite only non-Pass variants, address every coaching note,
    operator's voice from voice/clean/, rubric-coaches-you-write), **Loop and termination**
    (audit → revise → re-audit; stop when all Pass everywhere or after 3 total audit
    cycles; cycle-3 audit is last, no revision after it), **If non-Pass verdicts remain
    after cycle 3** (keep latest revision; log platform+category+verdict+why-unresolvable
    in notes.md), **Precedence** (DREAMS wins over anti-slop/voice/headline-craft STYLE
    rules; caps, 90–100% window, no-invented-facts, nickname-tag rules absolute), and
    **Re-verify char budgets after every revision** (headline_max / content_budget;
    tag appended exactly once after the final cycle).
  - Step 4b "exactly once" wording updated: anti-slop still runs once; DREAMS cycles are
    a separate later loop that never re-triggers it; variant not final until the loop ends.
  - Step 3 tag-timing sentence updated from "steps 4 and 5" to "steps 4, 5, and 6".
- Files changed:
  - ~/.claude/commands/generate-ad.md (OUTSIDE the repo — not git-tracked)
  - specs/dreams-validation.md (checked off US-002 criteria)
- Verification: npx tsc --noEmit from dashboard/ PASSED; grep confirmed all required
  phrases (revision pass, termination, 3-cycle cap, precedence, budget re-verify,
  updated 4b/step-3 wording) are present.
- Learnings for future iterations:
  - Did NOT touch the pristine backup workers/workspace/notes/generate-ad.md.bak-2026-07-10.
  - US-003 remains: Output section still has no dreams-review.md requirement, README
    Variants table has no DREAMS column, per-platform template has no ## DREAMS section,
    and the notes.md paragraph in ## Output still only lists step-1/2/2b/4 content —
    it needs the per-cycle DREAMS audit logs added.
  - The ## Output section starts at the "## Output" heading right after the new Step 6
    blocks; Variants table currently has columns Platform | Headline chars | Description chars.
---
## Iteration 3 - US-003: DREAMS scorecard in the output deliverables
- What was implemented (all in ~/.claude/commands/generate-ad.md, ## Output section):
  - New required deliverable `dreams-review.md` in OUTPUT_DIR with a full template:
    `Cycles used: <N>/3`, a final platform x six-category verdict table (D/R/E/A/M/S
    columns), and an `## Unresolved weaknesses` list ("None" when clean). Explicitly
    final-verdicts-only; per-cycle history stays in notes.md.
  - README.md Variants table gained a `DREAMS` column with example values (`Pass`,
    `2 weak`) and a definition line (Pass when all six categories pass, otherwise a
    count like "2 weak" / "1 missing").
  - Per-platform file template gained a one-line `## DREAMS` section (final verdict
    summary; example avoids em dashes since the template seeds real deliverables and
    em dashes are banned by the hard constraints).
  - notes.md paragraph in ## Output now also requires the per-cycle DREAMS audit logs
    from step 6 (one `## DREAMS audit — cycle <N>` block per cycle + the
    unresolved-categories list when the loop hits 3 cycles).
- Files changed:
  - ~/.claude/commands/generate-ad.md (OUTSIDE the repo — not git-tracked)
  - specs/dreams-validation.md (checked off US-003 criteria)
- Verification: npx tsc --noEmit from dashboard/ PASSED; grep confirmed dreams-review.md
  requirement, DREAMS table column, ## DREAMS section, and notes.md audit-log wording.
- Learnings for future iterations:
  - Pristine backup workers/workspace/notes/generate-ad.md.bak-2026-07-10 untouched.
  - Watch for em dashes in any example text added to the workflow templates — the hard
    constraints ban them in deliverables, and template examples get copied verbatim.
  - Only US-004 (Ship) remains: feature-log entry in specs/specs.md, readme.md §5.5
    update, PRD header to Implemented. Note specs/readme.md and specs/specs.md already
    have uncommitted modifications from the ad-posting branch work — stage carefully.
---
## Iteration 4 - US-004: Ship docs per the spec process
- What was implemented:
  - Added "DREAMS review loop" feature-log entry to specs/specs.md (what it does,
    files, output surface, invariants/precedence, how to extend).
  - specs/readme.md section 5.5 updated: DREAMS rubric added to Inputs, new
    "DREAMS review loop" bullet (inline, all variants, max 3 cycles, DREAMS-wins
    precedence), Output bullet now lists dreams-review.md; repo map gained
    .claude/skills/dreams-ad-review/.
  - PRD header flipped to **Status:** Implemented (2026-07-10); US-004 criteria
    checked off. File left at specs/dreams-validation.md (PRD notes the operator
    may rename it to specs/dreams-ad-review.md).
- Files changed: specs/specs.md, specs/readme.md, specs/dreams-validation.md,
  progress-dreams-validation.txt
- Verification: npx tsc --noEmit from dashboard/ PASSED.
- Learnings for future iterations:
  - specs/specs.md (untracked) and specs/readme.md carried uncommitted ad-posting
    ship-doc edits from earlier work on this branch; they were committed together
    with this ship commit since they are the same files on the same branch.
  - Feature complete: all acceptance criteria [x], status Implemented.
---
